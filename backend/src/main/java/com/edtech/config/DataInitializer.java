package com.edtech.config;

/*
 * DataInitializer foi removido e consolidado em {@link DatabaseSeeder}.
 *
 * <p>Motivo: existiam três seeders conflitantes (DataInitializer, DataSeeder,
 * DatabaseSeeder)
 * com dados duplicados e emails fora do domínio institucional permitido.
 *
 * @see DatabaseSeeder
 */
// Classe intencionalmente vazia â€” mantida para preservar histórico Git.
